# 2026-07-17 Working-Session Plan

**Available time:** 1 hour
**Gate at session start:** Proposal decisions require confirmation; baseline
gate not yet demonstrated
**Session owner:** Project lead
**Primary outcome:** Leave with an executable baseline path, named owners, and
a bounded continual-learning research dispatch

## Time boxes

| Elapsed time | Owner | Work | Required output |
|---|---|---|---|
| T+00–T+10 | Project lead; full team | Confirm PC as the primary candidate, BP as the principal baseline, Step 1 as static feasibility only, and domain-incremental MNIST as the protocol-study default | Decisions and unresolved items recorded; provisional protocol ID `mnist-domain-cl-v0.1` |
| T+10–T+25 | Baseline DRI; reproducibility DRI | Verify environment/submodule assumptions and run the dependency-free protocol checks and printed configuration | Command output, blocker list, and environment owner |
| T+25–T+40 | Baseline/PC/evaluator DRIs | Start the smallest Step 1 smoke run or confirm that it starts; inspect the expected run schema and assign stream, evaluator, and PC responsibilities | Smoke-run path or exact failure record; three named DRIs |
| T+40–T+55 | Project lead; CL research group | Review and accept the continual-learning research dispatch; assign Protocol, Metrics, and Evidence/Feasibility DRIs | Dispatch acknowledged and Monday handoff location named |
| T+55–T+60 | Full team | State blockers, Monday entry conditions, and who preserves run artifacts | Written handoff with one owner per blocker |

## Suggested verification commands

```bash
git submodule update --init --recursive
python3 -m unittest playground/step1-static-mnist/test_protocol.py
python3 playground/step1-static-mnist/experiment.py --print-config
```

Use the smoke command documented in
[`../playground/step1-static-mnist/README.md`](../playground/step1-static-mnist/README.md)
only after the checks above pass. Do not overwrite an existing run artifact.

## Exit criteria

- [ ] Primary candidate, baseline, and Step 1 interpretation are confirmed.
- [ ] Protocol tests/configuration either pass or have a named blocker.
- [ ] A smoke run has started, completed, or produced a preserved failure.
- [ ] Baseline, evaluator, PC, and CL-research DRIs are named.
- [ ] The CL research group accepts its Monday deadline and output format.

## Handoff

The CL group submits its recommendation no later than 15 minutes before the
July 20 session. The implementation group brings the Step 1 smoke artifact or
failure record. No full reference run is required in this one-hour session.
